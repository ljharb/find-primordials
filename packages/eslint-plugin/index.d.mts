// Type declarations for `eslint-plugin-find-primordials`.

import type { ESLint } from 'eslint';

/** The ESLint plugin: its `recommended`/`all` configs and the four primordial rules. */
declare const plugin: ESLint.Plugin;

export default plugin;
